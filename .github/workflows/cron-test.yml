name: Cron Test

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  cron-test:
    runs-on: ubuntu-latest
    steps:
      - name: Print time
        run: |
          echo "Cron test triggered"
          date -u
